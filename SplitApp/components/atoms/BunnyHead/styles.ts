import {StyleSheet} from 'react-native';
import {Colors} from '../../../resources/styleConstants';

const bunnyStyles = StyleSheet.create({
  main: {
    bottom: 240,
  },
});

export default bunnyStyles;
